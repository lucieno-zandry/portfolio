RewriteEngine On
RewriteBase /

# 1. If it's a real, existing file (like a CSS, JS, or image file), serve it directly
RewriteCond %{REQUEST_FILENAME} !-f

# 2. Route absolutely everything else (including the homepage and folders) to your router
RewriteRule ^(.*)$ /helpers/router.php [L,QSA]